<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>YouTube Shorts AI – Lifetime Access</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <style>
    body { font-family: 'Inter', sans-serif; }
    .fade-in { animation: fadeIn 0.8s ease-in forwards; opacity: 0; }
    @keyframes fadeIn { to { opacity: 1; } }
  </style>
</head>

<body class="bg-gradient-to-b from-yellow-50 to-white min-h-screen flex items-center justify-center">

  <div class="bg-white shadow-2xl rounded-3xl p-8 sm:p-12 w-full max-w-lg fade-in">

    <!-- Header -->
    <h1 class="text-4xl font-extrabold mb-4 text-red-600 text-center">
      YouTube Shorts AI
    </h1>

    <div class="flex justify-center gap-2 mb-6">
      <span class="bg-green-100 text-green-700 px-4 py-1 rounded-full text-sm font-semibold">
        🔓 Lifetime Access
      </span>
      <span class="bg-blue-100 text-blue-700 px-4 py-1 rounded-full text-sm font-semibold">
        💬 Lifetime Support
      </span>
    </div>

    <p class="text-gray-700 text-center mb-8">
      Pay once and get <strong>lifetime access</strong>.  
      No monthly fees. No renewals.
    </p>

    <!-- STEP 1 -->
    <div id="emailStep">
      <input
        id="userEmail"
        type="email"
        placeholder="Enter your email address"
        class="w-full p-4 mb-5 rounded-xl border border-gray-300 focus:ring-2 focus:ring-red-500 focus:outline-none"
      />

      <button
        id="continueBtn"
        class="w-full bg-red-600 text-white py-4 rounded-xl font-semibold hover:bg-red-700 transition">
        Continue
      </button>

      <p class="text-xs text-gray-500 text-center mt-4">
        🔒 Your email is safe. No spam.
      </p>
    </div>

    <!-- STEP 2 -->
    <div id="paymentStep" class="hidden fade-in">

      <p class="text-center mb-4 text-gray-700">
        Hi <span id="emailText" class="font-semibold"></span> 👋
      </p>

      <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-4 mb-6 text-center">
        <p class="font-bold text-lg">One-Time Payment</p>
        <p class="text-3xl font-extrabold text-red-600 mt-1">20,000 LKR</p>
        <p class="text-sm text-gray-600 mt-2">
          ✅ Lifetime Access<br>
          ✅ Lifetime Support Chat<br>
          ✅ Instant Activation
        </p>
      </div>

      <a
        id="payBtn"
        href="https://paylink.geniebiz.lk/XpkN9e8knL"
        target="_blank"
        class="block w-full text-center bg-red-600 text-white py-4 rounded-xl font-semibold hover:bg-red-700 transition">
        Pay & Continue
      </a>

      <p class="mt-4 text-center text-sm text-gray-600">
        After payment, send screenshot to  
        <a href="https://wa.me/94XXXXXXXXX" class="text-green-600 font-semibold">
          WhatsApp Support
        </a>
      </p>

    </div>

  </div>

  <!-- SCRIPT -->
  <script>
    const emailStep = document.getElementById("emailStep");
    const paymentStep = document.getElementById("paymentStep");
    const continueBtn = document.getElementById("continueBtn");
    const userEmail = document.getElementById("userEmail");
    const emailText = document.getElementById("emailText");

    continueBtn.addEventListener("click", () => {
      const email = userEmail.value.trim();

      if (!email || !email.includes("@")) {
        alert("Please enter a valid email address");
        return;
      }

      localStorage.setItem("yt_email", email);

      emailText.innerText = email;
      emailStep.classList.add("hidden");
      paymentStep.classList.remove("hidden");
    });

    window.onload = () => {
      const savedEmail = localStorage.getItem("yt_email");
      if (savedEmail) {
        emailText.innerText = savedEmail;
        emailStep.classList.add("hidden");
        paymentStep.classList.remove("hidden");
      }
    };
  </script>

</body>
</html>
